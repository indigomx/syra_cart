<div id="taxonomies" class="sidebar-item">
<% get_taxonomies.each do |taxonomy| %>
  <ul class="navigation-list">
	  <li<%= ' class="current"' if @taxon && @taxon == taxonomy.root %>><%= link_to "CATEGORIAS", seo_url(taxonomy.root).chop, :class => 'root' %></li>
	  <% taxonomy.root.children.each do |taxon| %>
	    <li<%= ' class="current"' if @taxon and ([@taxon] + @taxon.ancestors).include?(taxon) %>><%= link_to taxon.name, seo_url(taxon).chop %></li>
	  <% end %>
	</ul>
<% end %>
</div>
